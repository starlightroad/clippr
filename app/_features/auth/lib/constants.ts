export const MIN_PASSWORD = 8;

export const MAX_PASSWORD = 32;

export const PASSWORD_REGEX =
  /^(?=.*[a-zA-Z])(?=.*\d)(?=.*[!@#$%^&*()_+{}\[\]:;"'<>,.?/-]).+$/;

export const LOGIN_PAGE_METADATA = {
  TITLE: "Log in",
  DESCRIPTION:
    "Log in to your Clippr account to access and organize your favorite links.",
  KEYWORDS: ["sign in", "log in", "Clippr", "bookmark manager", "user login"],
};
